### 一. 后端开发框架与技术栈



本项目采用经典的分层架构，基于 Spring Boot 生态构建，确保了系统的稳定性与可扩展性。

- **核心框架**：`Spring Boot 2.7.18`
  - 提供自动化配置和依赖管理，快速搭建 RESTful API。
- **权限安全**：`Spring Security` + `JWT (JSON Web Token)`
  - 实现了无状态的认证机制，支持 Token 签发、校验及动态权限拦截。
- **数据库交互**：`MyBatis` + `MySQL 8.0`
  - 使用 XML Mapper 编写 SQL，灵活处理复杂的多表关联查询（如报告与水源、用户的关联）。
- **缓存与中间件**：`Redis` (Spring Data Redis)
  - 用途：用户 Session 缓存、分布式锁、接口限流、公开报告缓存（加速二维码访问）。
- **AI 集成**：`Dify Java Client`
  - 通过 SDK 与 Dify 平台的工作流对接，实现水质数据的智能分析。
- **工具组件**：
  - `ZXing`：生成报告二维码。
  - `ULID`：生成按时间排序的唯一字符串 ID（替代 UUID）。
  - `Swagger 3 (SpringFox)`：自动生成 API 接口文档。



这是一个完整的后端部署流程清单。你可以把它当作你的**“操作手册”**保存下来，下次部署时直接照着做即可。

------

### 二.后端部署流程

#### 1. 准备阶段（本地电脑）

1. **打包项目**：
   - 使用 Maven打包生成 `.jar` 文件。

------



### 2. 上传与配置阶段（服务器）

#### 

1. **创建项目目录**：

   Bash

   ```
   mkdir -p /var/sts  # 用于存放 jar 包和 Dockerfile
   mkdir -p /var/sts/uploads  # 用于存放上传的图片
   mkdir -p /var/sts/qrcode   # 用于存放二维码
   ```

2. **上传文件**：

   - 将 `STS-2.0.jar` 上传到 `/var/sts` 目录。

3. 创建 Dockerfile：

   在 /var/sts 目录下执行 vi Dockerfile，内容如下：

   Dockerfile

   ```
   # 使用轻量级且稳定的 JRE 基础镜像
   FROM eclipse-temurin:17-jre
   
   WORKDIR /app
   
   # 复制 jar 包并重命名为 app.jar
   COPY STS-2.0.jar app.jar
   
   # 暴露端口
   EXPOSE 8080
   
   # 启动命令
   ENTRYPOINT ["java", "-jar", "app.jar"]
   ```

------



### 3. 构建与启动阶段



1. 构建镜像：

   进入目录并构建（注意最后有个点）：

   Bash

   ```
   cd /var/sts
   docker build -t my-sts-app:v1 .
   ```

2. **启动容器（带数据卷挂载）**：

   Bash

   ```
   docker run -d \
     --name my-backend \
     --restart=always \
     -p 8080:8080 \
     -v /var/sts/uploads:/app/uploads \
     -v /var/sts/qrcode:/app/qrcode \
     -v /etc/localtime:/etc/localtime:ro \
     my-sts-app:v1
   ```