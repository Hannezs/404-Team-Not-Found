### 一. 前端开发框架与技术栈

本项目采用现代化的前端技术栈，基于 Vue.js 生态构建，确保了系统的高性能和良好的用户体验。

- **核心框架**：`Vue.js 2.6.14`
  - 前端核心框架，用于构建用户界面，实现组件化开发。
- **路由管理**：`Vue Router 3.6.5`
  - 官方路由管理器，用于实现单页应用的路由功能和页面导航。
- **UI 组件库**：`Element UI 2.15.14`
  - 基于 Vue 2.0 的桌面端组件库，提供丰富的 UI 组件，加速开发效率。
- **网络请求**：`Axios 1.13.2`
  - 基于 Promise 的 HTTP 客户端，用于发送请求到后端服务，支持拦截器和请求配置。
- **构建工具**：`Vue CLI 5.x` + `Webpack`
  - Vue.js 官方脚手架工具，用于项目初始化、开发和构建，底层使用 Webpack 进行模块打包。
- **代码规范**：`ESLint` + `Babel`
  - ESLint 用于代码检查，确保代码质量和一致性；Babel 用于转换 ES6+ 代码，使其兼容旧版浏览器。

### 二. 前端部署流程

#### 1. 开发环境部署

##### 前置条件
- Node.js 14.x 或更高版本
- npm 6.x 或更高版本
- Vue CLI 5.x

##### 安装步骤
1. **克隆项目代码**
   ```bash
   git clone <项目仓库地址>
   cd deep-water2
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **启动开发服务器**
   ```bash
   npm run serve
   ```
   开发服务器将在 http://localhost:7070 启动

#### 2. 生产环境部署

##### 构建项目
1. **执行构建命令**
   ```bash
   npm run build
   ```
   构建后的文件将生成在 `dist` 目录中

##### 2.1 Nginx 部署

1. **安装 Nginx**
   ```bash
   # Ubuntu/Debian
   apt-get update
   apt-get install nginx
   
   # CentOS/RHEL
   yum install nginx
   ```

2. **配置 Nginx 虚拟主机**
   在 /etc/nginx/conf.d/ 目录下创建 deep-water2.conf 文件：
   ```
   server {
       listen 80;
       server_name example.com;  # 替换为你的域名或IP
       
       root /var/www/deep-water2/dist;
       index index.html;
       
       location / {
           try_files $uri $uri/ /index.html;
       }
       
       # API 代理配置
       location /api {
           proxy_pass http://47.92.234.121:8080;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       }
   }
   ```

3. **重启 Nginx 服务**
   ```bash
   # Ubuntu/Debian
   systemctl restart nginx
   
   # CentOS/RHEL
   service nginx restart
   ```

##### 2.2 Docker 部署

1. **创建 Dockerfile**
   在项目根目录下创建 Dockerfile：
   ```
   FROM nginx:alpine
   
   COPY dist/ /usr/share/nginx/html/
   COPY nginx.conf /etc/nginx/conf.d/default.conf
   
   EXPOSE 80
   ```

2. **创建 nginx.conf**
   在项目根目录下创建 nginx.conf 文件：
   ```
   server {
       listen 80;
       server_name _;
       
       root /usr/share/nginx/html;
       index index.html;
       
       location / {
           try_files $uri $uri/ /index.html;
       }
       
       location /api {
           proxy_pass http://47.92.234.121:8080;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       }
   }
   ```

3. **构建 Docker 镜像**
   ```bash
   docker build -t deep-water2-frontend .
   ```

4. **运行 Docker 容器**
   ```bash
   docker run -d -p 80:80 --name deep-water2 deep-water2-frontend
   ```